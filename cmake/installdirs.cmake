if(WIN32)
    set(CMAKE_INSTALL_BINDIR "." CACHE PATH "")
    set(CMAKE_INSTALL_INCLUDEDIR "rdapi" CACHE PATH "")
    set(CMAKE_INSTALL_DATADIR "./" CACHE PATH "")
    set(REDASM_INSTALL_DIR "${CMAKE_INSTALL_BINDIR}" CACHE PATH "")
    set(REDASM_CMAKE_DIR "${REDASM_INSTALL_DIR}/rdapi/cmake" CACHE PATH "")
else()
    include(GNUInstallDirs)
    set(REDASM_INSTALL_DIR "${CMAKE_INSTALL_DATADIR}/redasm" CACHE PATH "")
    set(REDASM_CMAKE_DIR "${REDASM_INSTALL_DIR}/cmake" CACHE PATH "")
    set(REDASM_PLUGIN_PATH "${REDASM_INSTALL_DIR}/plugins")
endif()
