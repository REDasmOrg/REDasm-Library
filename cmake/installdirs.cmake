if(WIN32)
    set(REDASM_INSTALL_BINDIR "." CACHE PATH "")
    set(REDASM_INSTALL_INCLUDEDIR "rdapi" CACHE PATH "")
    set(REDASM_INSTALL_LIBDIR "." CACHE PATH )
    set(REDASM_INSTALL_PATH "" CACHE PATH "")
    set(REDASM_CMAKE_PATH "${REDASM_INSTALL_PATH}/rdapi/cmake" CACHE PATH "")
else()
    include(GNUInstallDirs)
    set(REDASM_INSTALL_PATH "${CMAKE_INSTALL_DATADIR}/redasm" CACHE PATH "")

    set(REDASM_INSTALL_BINDIR ${CMAKE_INSTALL_BINDIR} CACHE PATH "")
    set(REDASM_INSTALL_INCLUDEDIR ${CMAKE_INSTALL_INCLUDEDIR} CACHE PATH "")
    set(REDASM_INSTALL_LIBDIR ${CMAKE_INSTALL_LIBDIR} CACHE PATH "")
    set(REDASM_CMAKE_PATH "${REDASM_INSTALL_PATH}" CACHE PATH "")
    set(REDASM_PLUGIN_PATH "${REDASM_INSTALL_PATH}/plugins")
endif()
